# Hlavní framework pro webovou aplikaci
Django>=4.2.24

# CMS systém (Wagtail) pro správu obsahu
wagtail>=7.1,<7.2

# Autentizace a registrace uživatelů (včetně Microsoft OAuth)
django-allauth>=65.0.2

# Real-time komunikace pomocí Socket.IO
python-socketio>=5.10.0

# Asynchronní server pro Socket.IO
eventlet>=0.33.0

# PostgreSQL databázový adaptér
psycopg2-binary>=2.9.0

# Django rozšíření (např. pro generování modelových diagramů)
django-extensions==3.2.3

# Knihovny pro generování grafů (modelové diagramy)
pydotplus==2.0.2
graphviz==0.20.3